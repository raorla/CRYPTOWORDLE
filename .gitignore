# Node modules
node_modules/

# Compilation output
/dist
frontend/dist

# Test coverage output
/coverage

# Hardhat files
/artifacts
/cache

# Nox plugin offchain stack logs
/offchain-services.log

# Playwright e2e artifacts
test-results/
playwright-report/
blob-report/
.playwright/

# Secrets — never commit keys
.env
.env.local

# Local design references (kept on disk, not in the repo)
CryptoWordle design improvement/

# Local working notes & ops tooling (kept on disk, not in the repo)
CLAUDE.md
TODO.md
.claude/
daemon-ctl.sh

# Deployment records are committed on purpose (deployments/*.json), but logs are not
*.log

# macOS
.DS_Store
